[package]
name = "yowl"
version = "0.1.0"
license = "MIT"
edition = "2021"
readme = "README.md"

[dependencies]

[dev-dependencies]
pretty_assertions = "1.4.1"
criterion = "0.5.1"

# to appease -Zminimal-version
serde = { version = "1.0.100" }
regex = { version = "1.5.6" }

[[bench]]
name = "benchmark"
harness = false
